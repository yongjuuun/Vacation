package com.example.API.service.vacation;

import com.example.API.domain.user.User;
import com.example.API.domain.vacation.Vacation;
import com.example.API.web.dto.request.VacRequestDto;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
public interface VacationService {
//    List<Vacation> findAllVacation();           // 모든 휴가 요청 조회
//    Vacation findByVacationId(Long vacationId); // 특정 휴가 요청 조회
//    Vacation findByUserId(Long userId);         // 특정 유저 휴가 조회
//    Vacation requestVacation();                 // 휴가 신청
//    Vacation cancleVactionId(Long vacationId);  // 특정 휴가 취소
//    Long save(VacRequestDto vacRequestDto);     // 휴가 저장
//    Vacation getRemainDays(Long userId);        // 특정 유저 남은 휴가 조회


    // 어떤항목을 만들어야 하는지??
    // 1. 휴가 신청, 조회, 취소, 승인/반려
    // 2. 인자는 어떤거를 넣어야 하는지?
}
